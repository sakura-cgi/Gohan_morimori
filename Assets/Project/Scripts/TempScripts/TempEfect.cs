using UnityEngine;

public class TempEfect : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created


    // Update is called once per frame
    void Update()
    {
        if(GetComponent<TempManager>().temp <= 45)
        {
            
        }
    }
}
